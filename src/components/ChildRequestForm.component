<apex:component controller="TestRequestForm" selfClosing="true" allowDML="true" access="public">
    <apex:pageBlock >
        <apex:inputField value="{!webformChildRequest.Account_ID__c}"/>
        <apex:inputField value="{!webformChildRequest.Meter_Number_Meter_Badge_Number__c}"/>
        <apex:inputField value="{!webformChildRequest.Service_Point_ID__c}"/>
        <apex:commandButton action="{!insertRecord}" value="save"/>
    </apex:pageBlock>
</apex:component>